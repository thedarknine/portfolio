# yaml-language-server: $schema=../vendor/symfony/dependency-injection/Loader/schema/services.schema.json

# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.
# See also https://symfony.com/doc/current/service_container/import.html

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:
    app.images_dir: '%kernel.project_dir%/public/images'
    app.owner_name: 'Caroline Noyer'
    app.site_name: 'Caroline Noyer | Portfolio'
    app.owner_role: 'Technical Product Owner | Expertise SaaS & UX'
    app.site_url: 'https://carolinenoyer.fr'
    app.contact_email: 'studio@carolinenoyer.fr'
    # yamllint disable-line rule:line-length
    app.owner_description: '15 ans en développement web, aujourd’hui Product Owner technique. Je structure des produits SaaS alignés sur les besoins métiers et techniques. Découvrez mon parcours et mes réalisations.'
    app.cv_url: 'https://docs.carolinenoyer.fr/cnoyer-cv-productowner.pdf'

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true       # Automatically injects dependencies in your services.
        autoconfigure: true  # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones

    app.sitemap_subscriber:
        class: App\EventSubscriber\SitemapSubscriber
        arguments:
            $pageInfoRepository: '@App\Repository\PageInfoRepository'
        tags:
            - { name: kernel.event_subscriber }
