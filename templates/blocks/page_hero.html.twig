    <section class="nine-page-hero">
        <div class="container nine-hero-grid">
            <div class="nine-hero-content wow animate__animated animate__fadeInLeft">
                <span class="nine-section-label">{{ pagetitle }}</span>
                <h1 class="nine-tagline text-3xl font-bold mb-6">{{ subtitle }}</h1>
            </div>

            <div class="nine-hero-visual min-h-[200px] overflow-hidden wow animate__animated animate__fadeInRight">
                <img
                    src="images/{{ slug }}/hero-{{ slug }}.jpg"
                    alt="{{ pagetitle }}"
                    class="w-full object-cover"
                    style="clip-path: ellipse(100% 140% at 100% 50%);"
                />
            </div>
        </div>
    </section>